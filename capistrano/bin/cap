#!/bin/sh

export RUBYOPT=
export RUBYLIB=
ROOT=`dirname "$0"`
ROOT=`cd "$ROOT" &>/dev/null && pwd`

exec -a cap $ROOT/ruby-2.2.2-capistrano -r $ROOT/../extensions $ROOT/../app/bin/cap \
     -R $ROOT/../tasks \
     -R $ROOT/../app/lib/whenever/capistrano/v3/tasks \
     "$@"
